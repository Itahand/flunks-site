# Environment Variables for Local Development
# Copy this file to .env.local and configure as needed

# Build Mode Configuration
# Options: 'public' | 'build'
# - 'public': Production mode (limited features)
# - 'build': Development mode (all features including DeLorean tracker)
NEXT_PUBLIC_BUILD_MODE=build

# Other environment variables...
# NEXT_PUBLIC_FLOW_NETWORK=mainnet
# NEXT_PUBLIC_API_URL=http://localhost:3000